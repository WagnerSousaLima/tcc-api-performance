spring:
  application:
    name: tcc-api-performance
  datasource:
    url: jdbc:h2:mem:tccdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8085

